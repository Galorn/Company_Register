#include <iostream>

using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    return 0;

    /* PARTIE DE CHANGEMENT ID
    cout << "Etes vous sur de vouloir changer de manager ? OUI ou NON" << endl;
    cin >> Reponse;
    while (Reponse.compare("OUI") =! 0 && Reponse.compare("NON") =! 0)
    {
        cout << "Veuillez entrer une réponse valide "
    }
    if (reponse.compare("OUI") == 0 )
*/
}

